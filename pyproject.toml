[project]
name = "ai-learning"
version = "0.1.0"
description = "AI learning monorepo"
readme = "README.md"
requires-python = ">=3.14"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["projects/*"]
